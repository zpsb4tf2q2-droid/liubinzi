# Application Environment
# Values: development | test | production
NODE_ENV=development

# NextAuth Configuration
# URL where the application is hosted (required)
NEXTAUTH_URL=http://localhost:3000
# Secret used to encrypt JWT tokens (required, use a long random string)
NEXTAUTH_SECRET=changeme-super-secret-string

# Database Configuration
# Connection string for your database (required)
# For example, a local Postgres instance:
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/dbname?schema=public

# GitHub OAuth (Optional)
# Create a GitHub OAuth App at https://github.com/settings/developers
# Only required if you want to enable GitHub authentication
GITHUB_ID=
GITHUB_SECRET=

# Environment Validation
# Set to 1 to skip environment variable validation (not recommended)
# SKIP_ENV_VALIDATION=1
