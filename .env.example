# ----------------------------
# Core application settings
# ----------------------------
# Port used by the HTTP server when running locally.
PORT=3000
# URL that NextAuth uses to generate absolute callback URLs.
NEXTAUTH_URL=http://localhost:3000
# Secret used to encrypt NextAuth session tokens. Generate with `openssl rand -base64 32`.
NEXTAUTH_SECRET=change-me

# ----------------------------
# OAuth providers
# ----------------------------
# Google OAuth client ID for social login.
GOOGLE_CLIENT_ID=
# Google OAuth client secret for social login.
GOOGLE_CLIENT_SECRET=
# GitHub OAuth client ID for social login.
GITHUB_CLIENT_ID=
# GitHub OAuth client secret for social login.
GITHUB_CLIENT_SECRET=

# ----------------------------
# Database connectivity
# ----------------------------
# Primary database connection string (e.g., PostgreSQL connection URL).
DATABASE_URL=
# Direct connection string with elevated privileges for migrations, if required.
DATABASE_DIRECT_URL=

# ----------------------------
# Observability and error tracking
# ----------------------------
# Sentry DSN for error reporting.
SENTRY_DSN=
# Environment label reported to Sentry (e.g., development, staging, production).
SENTRY_ENVIRONMENT=development
