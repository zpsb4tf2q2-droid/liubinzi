# Environment
NODE_ENV=development

# NextAuth Configuration
# REQUIRED: The URL of your application (change in production)
NEXTAUTH_URL=http://localhost:3000

# REQUIRED: A secret key for encrypting tokens and sessions
# Generate a secure secret with: openssl rand -base64 32
# Or visit: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=changeme-please-generate-a-secure-32-char-minimum-secret

# Database Configuration
# REQUIRED: PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/authdb?schema=public

# GitHub OAuth (Optional)
# Get credentials from: https://github.com/settings/developers
GITHUB_ID=
GITHUB_SECRET=
