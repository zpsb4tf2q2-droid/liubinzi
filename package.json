{
  "name": "qumeihua-shijian-lixing",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "build": "node -e \"console.log('No build step defined yet')\"",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,cjs,mjs,jsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.0",
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=20"
  }
}
